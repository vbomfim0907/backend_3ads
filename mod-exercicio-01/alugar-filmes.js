export function alugarFilmes(filme, preço) {
    return `o filme ${filme} foi alugado corretamente pelo valor de ${preço}`
}

export function devolverFilmes(filme, preço) {
    return `o filme ${filme} foi devolvido corretamente pelo valor de ${preço}`
}
